const readline = require("readline");

const reader = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
});

// reader.question("What up", function(answer){
// if (answer.length >= 10){
//     console.log()
// g
// }

function canDrive(age)
if